/// <reference types="vite/client" />

interface ImportMetaEnv {
  /** ArcGIS Location Platform API key (Basemaps privilege) — licensed foraging-map tiles. */
  readonly VITE_ARCGIS_API_KEY?: string;
}
