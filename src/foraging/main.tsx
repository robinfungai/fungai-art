import React from 'react';
import ReactDOM from 'react-dom/client';
import ForagingApp from './ForagingApp';
import '../index.css';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <ForagingApp />
  </React.StrictMode>
);
